<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.biztabo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="15" />
    <!-- Network State Permissions to detect Internet status -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permission to vibrate -->
   <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="biztabo.activities.IntroSplashScreenActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="biztabo.activities.MainActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="biztabo.activities.FeaturedActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="biztabo.activities.SimpleSearchActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="biztabo.activities.ItemDescriptionActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="biztabo.activities.ShowMapActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="biztabo.activities.PromosAndEventsActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="biztabo.activities.AboutCebuActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="biztabo.activities.PromosAndEventsDescription"
            android:label="@string/app_name" >
        </activity>
    </application>

</manifest>